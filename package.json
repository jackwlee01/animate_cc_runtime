{
  "name": "animate_cc_runtime",
  "version": "1.0.0",
  "description": "",
  "main": "test-page.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=bin/bundle.js",
    "build-minimal": "esbuild src/examples/minimal.ts --bundle --outfile=bin/minimal.js --sourcemap",
    "build-symbol-explorer": "esbuild src/examples/symbol-explorer.ts --bundle --outfile=bin/symbol-explorer.js --sourcemap",
    "build-page": "npm run build-symbol-explorer && npm run build-minimal",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "live-server bin"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.5",
    "live-server": "^1.2.1"
  }
}
